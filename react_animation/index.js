/**
 * @format
 */

import {AppRegistry} from 'react-native';
import SwipeAnimation from './SwipeAnimation';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => SwipeAnimation);
